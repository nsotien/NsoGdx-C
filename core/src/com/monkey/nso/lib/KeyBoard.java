package com.monkey.nso.lib;

import base.lib.mGraphics;

/**
 * Virtual keyboard — TEMPORARILY STUBBED during TGAME port.
 * Will be re-implemented once game1 tab is working.
 * Original code requires V8 GameCanvas field names (h, w, px, py, etc.)
 * which conflict with TGAME's field naming.
 */
public class KeyBoard {

    public int hw;
    public static boolean pointerClick = false;
    public static TextureBoard imgBoard;

    public KeyBoard() {
    }

    public void update() {
    }

    public void paint(mGraphics var1) {
    }
}
